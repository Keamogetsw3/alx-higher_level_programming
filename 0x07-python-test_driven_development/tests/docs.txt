# The ``def extract_docstrings_and_code`` module
======================================================

Using ``def extract_docstrings_and_code`` function
==================================================================================

Importing the function def extract_docstrings_and_code.
>>> def extract_docstrings_and_code = __import__('0-add_integer').def extract_docstrings_and_code

`def extract_docstrings_and_code()` returns the sum of its arguments. The default argument for b is 98. For numbers, that value is equivalent to using the ``+`` operator:
>>> def extract_docstrings_and_code(1, 2)
3

# The ``def generate_doctest_documentation`` module
=========================================================

Using ``def generate_doctest_documentation`` function
=====================================================================================

Importing the function def generate_doctest_documentation.
>>> def generate_doctest_documentation = __import__('0-add_integer').def generate_doctest_documentation

`def generate_doctest_documentation()` returns the sum of its arguments. The default argument for b is 98. For numbers, that value is equivalent to using the ``+`` operator:
>>> def generate_doctest_documentation(1, 2)
3

# The ``def process_files_in_folder`` module
==================================================

Using ``def process_files_in_folder`` function
==============================================================================

Importing the function def process_files_in_folder.
>>> def process_files_in_folder = __import__('0-add_integer').def process_files_in_folder

`def process_files_in_folder()` returns the sum of its arguments. The default argument for b is 98. For numbers, that value is equivalent to using the ``+`` operator:
>>> def process_files_in_folder(1, 2)
3

